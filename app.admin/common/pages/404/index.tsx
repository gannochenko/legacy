import React from 'react';

import Layout from '../../components/Layout';

const NotFoundPage = () => {
    return <Layout title="404 - not found">This page was not found</Layout>;
};

export default NotFoundPage;
