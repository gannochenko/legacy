/**
 * https://github.com/sapegin/jest-cheat-sheet
 */

import Migrator from '../migrator';

describe('Migrator', () => {
    // beforeAll(async () => {
    // });
    // beforeEach(async () => {
    // });
    // afterEach(async () => {
    // });
    describe('getDelta()', () => {
        it('should return initial delta', async () => {
            throw new Error('Todo');
        });
        it('should return delta relative to the current situation', async () => {
            throw new Error('Todo');
        });
    });
});
