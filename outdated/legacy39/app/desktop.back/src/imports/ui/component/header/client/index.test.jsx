/* eslint-env mocha */
/* eslint-disable no-unused-expressions */

import React from 'react';
// import expect, {mount} from '../../../lib/util/test-env/index.client.js';

// import Header from './index.jsx';

describe('Header', () => {
    before(() => {
        // utilUserDeprecated.cleanDBUsers();
        // utilUser.createAdmin({authorize: true});
    });

    // afterEach(() => {
    // });

    after(() => {
        // return utilUser.logout();
    });

    it('renders', () => {
        // global.window = { location : { host : 'example.com' } };
        // const page = mount(<Header />);
        // expect(page.find('.header__logo')).to.exist;
    });
});
