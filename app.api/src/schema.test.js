describe('Schema', () => {
    beforeAll(() => {
        console.dir('Before!');
    });
    beforeEach(() => {
        console.dir('Before each');
    });
    it('should work', () => {
        console.dir('lala');
    });
});
