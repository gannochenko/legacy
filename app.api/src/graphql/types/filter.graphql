enum FilterLogic {
    AND
    OR
    NAND
    NOR
}

enum FilterOperator {
    EQ
    NE
    IN
    NIN
    GT
    LT
    GTE
    LTE
}

input IFilterFieldValue {
    Operation: FilterOperator
    Value: String
    Values: [String]
}
